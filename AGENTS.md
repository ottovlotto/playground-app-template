# AI agent guidance

Minimal Polkadot dapp template (React + Vite + TypeScript, Host API wired for account access from Polkadot Desktop). Canonical agent guidance is in [CLAUDE.md](CLAUDE.md) — read that first.
